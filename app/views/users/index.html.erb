<div class= "col-xs-3">
<%= render 'books/list', books: @books, user: @user, book: @book %>
</div>

<div class= "col-xs-9">
<h1>Users</h1>

<table class="table table-striped">
	<thead>
      <tr>
        <th>imege</th>
     <th>name</th>
     <th></th>
   </tr>
 </thead>

<tbody>

   <% @users.each do |user| %>

    <tr>
    	<td><%= attachment_image_tag user, :profile_image, :fill, 100, 100, fallback: "no_image.jpg" %></td>
      <td><%= user.name %></td>
      <td><%=user.introduction %></td>
      <td><%= link_to "Show", user_path(user.id) %></td>

      </tr>
    <% end %>
  </tbody>
</table>
</div>
